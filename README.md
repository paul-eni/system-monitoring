# system-monitoring
A Bash-based monitoring script for a minimalist Ubuntu server that collects key metrics (such RAM and network information for instance) using only native Linux system files and commands (e.g. /proc...), without external dependencies.
